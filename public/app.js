const $ = (selector) => document.querySelector(selector);
const $$ = (selector) => [...document.querySelectorAll(selector)];
let state;
const visiblePeople = { schedule: new Set(), calendar: new Set() };
let calendarDate = new Date(new Date().getFullYear(), new Date().getMonth(), 1);
const initials = (name) => name.split(/\s+/).map((part) => part[0]).slice(0, 2).join('').toUpperCase();
const escapeHtml = (value = '') => value.replace(/[&<>'"]/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' }[char]));
const formatDate = (value) => new Intl.DateTimeFormat('en', { day: 'numeric', month: 'short' }).format(new Date(value));
function toast(message, error = false) { const el = $('#toast'); el.textContent = message; el.className = `toast show${error ? ' error' : ''}`; setTimeout(() => el.className = 'toast', 2600); }
async function api(url, options = {}) { const response = await fetch(url, { headers: { 'Content-Type': 'application/json' }, ...options }); if (!response.ok) { const body = await response.json().catch(() => ({})); throw new Error(body.error || 'Request failed'); } return response.status === 204 ? null : response.json(); }

async function boot() {
  const config = await api('/api/config'); if (config.ldapEnabled) $('#provider-wrap').classList.remove('hidden');
  try { await api('/api/me'); await enterApp(); } catch { $('#auth').classList.remove('hidden'); }
}
async function enterApp() { state = await api('/api/state'); $('#auth').classList.add('hidden'); $('#app').classList.remove('hidden'); render(); }
function render() {
  const me = state.currentUser; $('#header-name').textContent = me.displayName; $('#first-name').textContent = me.displayName.split(' ')[0];
  $('#header-avatar').textContent = initials(me.displayName); $('#header-role').textContent = me.permissions.includes('Admin') ? 'Administrator' : 'Team member';
  $('#permissions-tab').classList.toggle('hidden', !me.permissions.includes('Admin')); $('#scheduler-hint').classList.toggle('hidden', !me.permissions.includes('Scheduler'));
  const people = $('#people'); people.innerHTML = state.users.map((user) => `<button class="person" data-person="${user.id}"><span class="avatar">${initials(user.displayName)}</span><span><b>${escapeHtml(user.displayName)}</b><span class="status">Available</span></span><span>›</span></button>`).join('');
  $('#people-count').textContent = `${state.users.length} ${state.users.length === 1 ? 'person' : 'people'}`;
  $('#resource-grid').innerHTML = state.users.map((user) => `<article class="resource-card">${me.permissions.includes('Manage resources') ? `<button class="card-action" data-edit-resource="${user.id}" aria-label="Edit ${escapeHtml(user.displayName)}">✎</button>` : ''}<div class="avatar">${initials(user.displayName)}</div><h3>${escapeHtml(user.displayName)}</h3><p class="muted">${escapeHtml(user.email || user.username)}</p><div class="working">● Available today · Standard hours</div></article>`).join('');
  $('#team-grid').innerHTML = state.teams.length ? state.teams.map((team) => `<article class="team-card">${me.permissions.includes('Manage teams') ? `<button class="card-action" data-edit-team="${team.id}" aria-label="Edit ${escapeHtml(team.name)}">✎</button>` : ''}<p class="eyebrow">${team.members.length} MEMBERS</p><h3>${escapeHtml(team.name)}</h3><p class="muted">${escapeHtml(team.description || 'No description')}</p><div class="working">${team.members.map((m) => escapeHtml(state.users.find((u) => u.id === m.userId)?.displayName || '')).join(' · ')}</div></article>`).join('') : '<div class="empty">No teams yet. Create your first team to get organised.</div>';
  const shownSchedule = visiblePeople.schedule.size ? visiblePeople.schedule : new Set(state.users.map((user) => user.id));
  const events = state.tasks.filter((event) => shownSchedule.has(event.userId)).map((e) => ({ ...e, kind: 'Task' })).sort((a, b) => new Date(a.start) - new Date(b.start)).slice(0, 5);
  $('#upcoming-list').innerHTML = events.length ? events.map((e) => `<div class="event"><div class="event-date">${formatDate(e.start).split(' ')[1]}<b>${formatDate(e.start).split(' ')[0]}</b></div><div><b>${escapeHtml(e.title)}</b><div class="muted">${e.kind} · ${escapeHtml(state.users.find((u) => u.id === e.userId)?.displayName || '')}</div></div>${e.kind === 'Task' && me.permissions.includes('Scheduler') ? `<div class="event-actions"><button class="event-action" data-edit-task="${e.id}" aria-label="Edit ${escapeHtml(e.title)}">✎</button><button class="event-action delete" data-delete-task="${e.id}" aria-label="Remove ${escapeHtml(e.title)}">×</button></div>` : ''}</div>`).join('') : '<div class="empty">Your schedule is beautifully clear.</div>';
  $('#holiday-user').innerHTML = state.users.filter((u) => u.id === me.id || me.permissions.includes('Scheduler')).map((u) => `<option value="${u.id}">${escapeHtml(u.displayName)}</option>`).join('');
  renderTeamMembers(); renderPermissionUsers(); renderCalendar(); renderHolidays(); updateFilterLabels(); tick();
}
function updateFilterLabels() { $$('[data-filter]').forEach((button) => { const count = visiblePeople[button.dataset.filter].size; button.textContent = count ? `${count} selected ▾` : 'Everyone ▾'; }); }
function renderCalendar() {
  const allowed = visiblePeople.calendar.size ? visiblePeople.calendar : new Set(state.users.map((user) => user.id));
  const year = calendarDate.getFullYear(); const month = calendarDate.getMonth(); const firstOffset = (new Date(year, month, 1).getDay() + 6) % 7; const days = new Date(year, month + 1, 0).getDate();
  const events = [...state.tasks.map((item) => ({ ...item, label: item.title, kind: 'task' })), ...state.holidays.map((item) => ({ ...item, label: item.note || 'Holiday', kind: 'holiday' }))].filter((item) => allowed.has(item.userId));
  $('#calendar-view').innerHTML = `<div class="calendar-title"><h2>${calendarDate.toLocaleDateString([], { month: 'long', year: 'numeric' })}</h2></div><div class="weekdays">${['Mon','Tue','Wed','Thu','Fri','Sat','Sun'].map((day) => `<b>${day}</b>`).join('')}</div><div class="month-grid">${Array.from({ length: firstOffset }, () => '<div class="calendar-day outside"></div>').join('')}${Array.from({ length: days }, (_, index) => { const day = index + 1; const date = new Date(year, month, day); const dateKey = `${year}-${String(month + 1).padStart(2, '0')}-${String(day).padStart(2, '0')}`; const matches = events.filter((item) => dateKey >= item.start.slice(0, 10) && dateKey <= item.end.slice(0, 10)); return `<div class="calendar-day${date.toDateString() === new Date().toDateString() ? ' today' : ''}"><span>${day}</span>${matches.slice(0, 3).map((item) => `<div class="calendar-event ${item.kind}" title="${escapeHtml(item.label)}">${escapeHtml(item.label)} · ${escapeHtml(state.users.find((user) => user.id === item.userId)?.displayName || '')}</div>`).join('')}${matches.length > 3 ? `<small>+${matches.length - 3} more</small>` : ''}</div>`; }).join('')}</div>`;
}
function renderHolidays() {
  const canSchedule = state.currentUser.permissions.includes('Scheduler'); const holidays = [...state.holidays].sort((a, b) => new Date(a.start) - new Date(b.start));
  $('#holiday-list').innerHTML = holidays.length ? holidays.map((item) => { const user = state.users.find((u) => u.id === item.userId); const editable = canSchedule || item.userId === state.currentUser.id; return `<article class="holiday-card"><div class="avatar">${initials(user?.displayName || '?')}</div><div><p class="eyebrow">${formatDate(item.start)} — ${formatDate(item.end)}</p><h3>${escapeHtml(item.note || 'Holiday')}</h3><p class="muted">${escapeHtml(user?.displayName || 'Unknown person')}</p></div>${editable ? `<div class="holiday-actions"><button data-edit-holiday="${item.id}" aria-label="Edit holiday">✎</button><button data-delete-holiday="${item.id}" aria-label="Delete holiday">×</button></div>` : ''}</article>`; }).join('') : '<div class="empty">No holidays have been added yet.</div>';
}
function renderTeamMembers() { $('#team-members').innerHTML = state.users.map((u) => `<div class="member-option"><label><input type="checkbox" name="member" value="${u.id}"> <span>${escapeHtml(u.displayName)}</span></label><div class="schedule hidden"><label>Start<input type="time" data-start="${u.id}" value="09:00"></label><label>End<input type="time" data-end="${u.id}" value="17:00"></label><label>Days<select data-days="${u.id}"><option>Mon–Fri</option><option>Every day</option><option>Custom / flexible</option></select></label></div></div>`).join(''); }
function renderPermissionUsers() { if (!state.currentUser.permissions.includes('Admin')) return; $('#permission-users').innerHTML = state.users.map((u, i) => `<button class="user-select${i === 0 ? ' active' : ''}" data-user="${u.id}"><span class="avatar">${initials(u.displayName)}</span><span><b>${escapeHtml(u.displayName)}</b><small>${escapeHtml(u.username)}</small></span></button>`).join(''); if (state.users[0]) renderPermissionEditor(state.users[0].id); }
function renderPermissionEditor(id) { const user = state.users.find((u) => u.id === id); const descriptions = { Admin: 'Manage users and workspace permissions.', Scheduler: 'Add tasks and leave to other people’s calendars.', 'Manage teams': 'Create teams and set working patterns.', 'Manage resources': 'Create and update shared resources.' }; $('#permission-editor').innerHTML = `<p class="eyebrow">ACCESS FOR</p><h2>${escapeHtml(user.displayName)}</h2><p class="muted">Toggle access below. Every change is saved automatically.</p>${state.permissions.map((p) => `<label class="permission-row"><span><b>${p}</b><p>${descriptions[p]}</p></span><input class="switch" type="checkbox" data-permission="${p}" data-id="${id}" ${user.permissions.includes(p) ? 'checked' : ''}></label>`).join('')}`; }

$('#auth-toggle').addEventListener('click', () => { const register = $('#register-form').classList.toggle('hidden'); $('#login-form').classList.toggle('hidden'); $('#auth-title').textContent = register ? 'Sign in to your workspace' : 'Create your workspace'; $('#auth-copy').textContent = register ? 'Use your local account to continue.' : 'The first account becomes workspace administrator.'; $('#auth-toggle').innerHTML = register ? 'New here? <b>Create an account</b>' : 'Already registered? <b>Sign in</b>'; });
for (const [id, endpoint] of [['login-form', '/api/login'], ['register-form', '/api/register']]) $(`#${id}`).addEventListener('submit', async (event) => { event.preventDefault(); try { const body = Object.fromEntries(new FormData(event.target)); await api(endpoint, { method: 'POST', body: JSON.stringify(body) }); await enterApp(); } catch (e) { toast(e.message, true); } });
$('#logout').addEventListener('click', async () => { await api('/api/logout', { method: 'POST' }); location.reload(); });
document.addEventListener('click', (event) => {
  const nav = event.target.closest('[data-page]'); if (nav) { $$('.page').forEach((p) => p.classList.add('hidden')); $(`#${nav.dataset.page}`).classList.remove('hidden'); $$('nav button').forEach((b) => b.classList.toggle('active', b === nav)); }
  const modal = event.target.closest('[data-modal]'); if (modal) $(`#${modal.dataset.modal}-modal`).showModal();
  const person = event.target.closest('[data-person]'); if (person && state.currentUser.permissions.includes('Scheduler')) { const user = state.users.find((u) => u.id === person.dataset.person); $('#task-form [name=userId]').value = user.id; $('.selected-person').textContent = user.displayName; $('#task-modal').showModal(); }
  const userButton = event.target.closest('[data-user]'); if (userButton) { $$('.user-select').forEach((b) => b.classList.toggle('active', b === userButton)); renderPermissionEditor(userButton.dataset.user); }
  const editTask = event.target.closest('[data-edit-task]'); if (editTask) openEditor('task', state.tasks.find((item) => item.id === editTask.dataset.editTask));
  const editResource = event.target.closest('[data-edit-resource]'); if (editResource) openEditor('resource', state.users.find((item) => item.id === editResource.dataset.editResource));
  const editTeam = event.target.closest('[data-edit-team]'); if (editTeam) openEditor('team', state.teams.find((item) => item.id === editTeam.dataset.editTeam));
  const editHoliday = event.target.closest('[data-edit-holiday]'); if (editHoliday) openEditor('holiday', state.holidays.find((item) => item.id === editHoliday.dataset.editHoliday));
  const deleteTask = event.target.closest('[data-delete-task]'); if (deleteTask) removeTask(deleteTask.dataset.deleteTask);
  const deleteHoliday = event.target.closest('[data-delete-holiday]'); if (deleteHoliday) removeHoliday(deleteHoliday.dataset.deleteHoliday);
  const filter = event.target.closest('[data-filter]'); if (filter) openFilter(filter.dataset.filter);
  const shift = event.target.closest('[data-shift]'); if (shift) { calendarDate = new Date(calendarDate.getFullYear(), calendarDate.getMonth() + Number(shift.dataset.shift), 1); renderCalendar(); }
  const clockSettings = event.target.closest('.clock-settings'); if (clockSettings) openClockEditor(clockSettings.closest('.clock'));
});
document.addEventListener('change', async (event) => {
  if (event.target.name === 'member') event.target.closest('.member-option').querySelector('.schedule').classList.toggle('hidden', !event.target.checked);
  if (event.target.name === 'edit-member') event.target.closest('.member-option').querySelector('.schedule').classList.toggle('hidden', !event.target.checked);
  if (event.target.matches('[data-permission]')) { const user = state.users.find((u) => u.id === event.target.dataset.id); const permissions = event.target.checked ? [...new Set([...user.permissions, event.target.dataset.permission])] : user.permissions.filter((p) => p !== event.target.dataset.permission); try { const updated = await api(`/api/users/${user.id}/permissions`, { method: 'PUT', body: JSON.stringify({ permissions }) }); Object.assign(user, updated); if (user.id === state.currentUser.id) state.currentUser = updated; toast('Permission updated'); } catch (e) { event.target.checked = !event.target.checked; toast(e.message, true); } }
});
async function submitDialog(form, endpoint, transform) { form.addEventListener('submit', async (event) => { event.preventDefault(); try { let body = Object.fromEntries(new FormData(form)); if (transform) body = transform(body, form); await api(endpoint, { method: 'POST', body: JSON.stringify(body) }); form.closest('dialog').close(); form.reset(); state = await api('/api/state'); render(); toast('Saved successfully'); } catch (e) { toast(e.message, true); } }); }
submitDialog($('#task-form'), '/api/tasks'); submitDialog($('#holiday-form'), '/api/holidays'); submitDialog($('#team-form'), '/api/teams', (body, form) => ({ name: body.name, description: body.description, members: $$('input[name=member]:checked', form).map((input) => ({ userId: input.value, start: form.querySelector(`[data-start="${input.value}"]`).value, end: form.querySelector(`[data-end="${input.value}"]`).value, days: form.querySelector(`[data-days="${input.value}"]`).value })) }));

function openFilter(target) { $('#filter-form [name=target]').value = target; $('#filter-people').innerHTML = state.users.map((user) => `<label class="filter-person"><input type="checkbox" name="person" value="${user.id}" ${!visiblePeople[target].size || visiblePeople[target].has(user.id) ? 'checked' : ''}><span class="avatar">${initials(user.displayName)}</span><b>${escapeHtml(user.displayName)}</b></label>`).join(''); $('#filter-modal').showModal(); }
$('#filter-form').addEventListener('submit', (event) => { event.preventDefault(); const target = event.target.elements.target.value; const selected = $$('input[name=person]:checked', event.target).map((input) => input.value); visiblePeople[target] = selected.length === state.users.length ? new Set() : new Set(selected); $('#filter-modal').close(); render(); });
const timezones = ['UTC', 'Europe/London', 'Europe/Paris', 'America/New_York', 'America/Chicago', 'America/Los_Angeles', 'Asia/Dubai', 'Asia/Kolkata', 'Asia/Singapore', 'Asia/Tokyo', 'Australia/Sydney'];
function openClockEditor(widget) { const form = $('#clock-form'); form.elements.instance.value = widget.dataset.instance; form.elements.label.value = widget.dataset.label || 'Local time'; form.elements.timezone.innerHTML = timezones.map((zone) => `<option ${zone === (widget.dataset.timezone || Intl.DateTimeFormat().resolvedOptions().timeZone) ? 'selected' : ''}>${zone}</option>`).join(''); $('#clock-modal').showModal(); }
$('#clock-form').addEventListener('submit', (event) => { event.preventDefault(); const widget = $(`.widget[data-instance="${CSS.escape(event.target.elements.instance.value)}"]`, grid); widget.dataset.label = event.target.elements.label.value; widget.dataset.timezone = event.target.elements.timezone.value; $('#clock-modal').close(); saveWidgetLayout(); tick(); toast('Clock updated'); });

function localDateTime(value) { const date = new Date(value); return new Date(date.getTime() - date.getTimezoneOffset() * 60000).toISOString().slice(0, 16); }
function openEditor(kind, item) {
  if (!item) return;
  const form = $('#edit-form'); form.reset(); form.elements.id.value = item.id; form.elements.kind.value = kind;
  const titles = { task: ['EDIT SCHEDULE', 'Edit task'], resource: ['EDIT RESOURCE', 'Edit resource'], team: ['EDIT TEAM', 'Edit team'], holiday: ['EDIT HOLIDAY', 'Edit holiday'] };
  [$('#edit-eyebrow').textContent, $('#edit-title').textContent] = titles[kind];
  if (kind === 'task') $('#edit-fields').innerHTML = `<label>Team member<select name="userId">${state.users.map((u) => `<option value="${u.id}" ${u.id === item.userId ? 'selected' : ''}>${escapeHtml(u.displayName)}</option>`).join('')}</select></label><label>Task title<input name="title" required value="${escapeHtml(item.title)}"></label><div class="form-row"><label>Starts<input name="start" type="datetime-local" required value="${localDateTime(item.start)}"></label><label>Ends<input name="end" type="datetime-local" required value="${localDateTime(item.end)}"></label></div><label>Notes<textarea name="notes">${escapeHtml(item.notes || '')}</textarea></label>`;
  if (kind === 'resource') $('#edit-fields').innerHTML = `<label>Full name<input name="displayName" required value="${escapeHtml(item.displayName)}"></label><label>Email<input name="email" type="email" value="${escapeHtml(item.email || '')}" placeholder="name@company.com"></label>`;
  if (kind === 'team') $('#edit-fields').innerHTML = `<label>Team name<input name="name" required value="${escapeHtml(item.name)}"></label><label>Description<input name="description" value="${escapeHtml(item.description || '')}"></label><p class="label">Members & working patterns</p>${teamMemberFields(item.members)}`;
  if (kind === 'holiday') $('#edit-fields').innerHTML = `<label>Team member<select name="userId">${state.users.map((u) => `<option value="${u.id}" ${u.id === item.userId ? 'selected' : ''}>${escapeHtml(u.displayName)}</option>`).join('')}</select></label><div class="form-row"><label>First day<input name="start" type="date" required value="${item.start.slice(0, 10)}"></label><label>Last day<input name="end" type="date" required value="${item.end.slice(0, 10)}"></label></div><label>Note<input name="note" value="${escapeHtml(item.note || '')}"></label>`;
  $('#edit-modal').showModal();
}
$('#edit-form').addEventListener('submit', async (event) => {
  event.preventDefault(); const body = Object.fromEntries(new FormData(event.target)); const { id, kind } = body; delete body.id; delete body.kind;
  if (kind === 'team') body.members = $$('input[name=edit-member]:checked', event.target).map((input) => ({ userId: input.value, start: event.target.querySelector(`[data-edit-start="${input.value}"]`).value, end: event.target.querySelector(`[data-edit-end="${input.value}"]`).value, days: event.target.querySelector(`[data-edit-days="${input.value}"]`).value }));
  const endpoint = kind === 'resource' ? `/api/users/${id}` : `/api/${kind}s/${id}`;
  try { await api(endpoint, { method: 'PUT', body: JSON.stringify(body) }); $('#edit-modal').close(); state = await api('/api/state'); render(); toast('Changes saved'); } catch (error) { toast(error.message, true); }
});
async function removeTask(id) { if (!confirm('Remove this scheduled task?')) return; try { await api(`/api/tasks/${id}`, { method: 'DELETE' }); state = await api('/api/state'); render(); toast('Task removed'); } catch (error) { toast(error.message, true); } }
async function removeHoliday(id) { if (!confirm('Delete this holiday?')) return; try { await api(`/api/holidays/${id}`, { method: 'DELETE' }); state = await api('/api/state'); render(); toast('Holiday removed'); } catch (error) { toast(error.message, true); } }
function teamMemberFields(members = []) { return `<div class="edit-members">${state.users.map((user) => { const member = members.find((entry) => entry.userId === user.id); return `<div class="member-option"><label><input type="checkbox" name="edit-member" value="${user.id}" ${member ? 'checked' : ''}> <span>${escapeHtml(user.displayName)}</span></label><div class="schedule${member ? '' : ' hidden'}"><label>Start<input type="time" data-edit-start="${user.id}" value="${member?.start || '09:00'}"></label><label>End<input type="time" data-edit-end="${user.id}" value="${member?.end || '17:00'}"></label><label>Days<select data-edit-days="${user.id}"><option ${member?.days === 'Mon–Fri' ? 'selected' : ''}>Mon–Fri</option><option ${member?.days === 'Every day' ? 'selected' : ''}>Every day</option><option ${member?.days === 'Custom / flexible' ? 'selected' : ''}>Custom / flexible</option></select></label></div></div>`; }).join('')}</div>`; }
const grid = $('.dashboard-grid');
function widgetLayout() { return $$('.widget', grid).map((widget) => ({ key: widget.dataset.instance || widget.dataset.widget, type: widget.dataset.widget, cols: Number(widget.dataset.cols || 2), rows: Number(widget.dataset.rows || (widget.dataset.widget === 'people' ? 4 : 2)), col: Number(widget.dataset.col || 0), row: Number(widget.dataset.row || 0), label: widget.dataset.label, timezone: widget.dataset.timezone })); }
function saveWidgetLayout() { localStorage.setItem('fineResourcer.widgets', JSON.stringify(widgetLayout())); }
function prepareWidget(widget) {
  if (!widget.dataset.instance) widget.dataset.instance = widget.dataset.widget;
  widget.style.gridColumn = `span ${widget.dataset.cols || 2}`; widget.style.gridRow = `span ${widget.dataset.rows || (widget.dataset.widget === 'people' ? 4 : 2)}`;
  if (widget.dataset.col) widget.style.gridColumn = `${widget.dataset.col} / span ${widget.dataset.cols || 2}`;
  if (widget.dataset.row) widget.style.gridRow = `${widget.dataset.row} / span ${widget.dataset.rows || 2}`;
  if (!widget.querySelector('.resize-handle')) { const handle = document.createElement('button'); handle.type = 'button'; handle.className = 'resize-handle'; handle.setAttribute('aria-label', 'Resize widget'); widget.append(handle); }
}
function restoreWidgets() {
  const saved = JSON.parse(localStorage.getItem('fineResourcer.widgets') || 'null');
  $$('.widget', grid).forEach(prepareWidget); if (!saved) return;
  const templates = Object.fromEntries($$('.widget', grid).map((widget) => [widget.dataset.widget, widget]));
  $$('.widget', grid).forEach((widget) => widget.remove());
  saved.forEach((entry) => { let widget = templates[entry.type]; if (!widget || widget.isConnected) { widget = templates[entry.type].cloneNode(true); widget.querySelectorAll('[id]').forEach((node) => node.removeAttribute('id')); } Object.entries(entry).forEach(([key, value]) => { if (value !== undefined && key !== 'key') widget.dataset[key] = value; }); widget.dataset.instance = entry.key; prepareWidget(widget); grid.insertBefore(widget, $('.add-widget', grid)); });
}
$('#widget-form').addEventListener('submit', (event) => {
  event.preventDefault(); const type = event.submitter?.value; const source = $(`.widget[data-widget="${type}"]`, grid); if (!source) return;
  const widget = source.cloneNode(true); widget.querySelectorAll('[id]').forEach((node) => node.removeAttribute('id')); widget.dataset.instance = `${type}-${Date.now()}`; prepareWidget(widget); grid.insertBefore(widget, $('.add-widget', grid)); $('#widget-modal').close(); saveWidgetLayout(); widget.animate([{ opacity: 0, transform: 'scale(.94)' }, { opacity: 1, transform: 'none' }], { duration: 240 });
});
grid.addEventListener('pointerdown', (event) => {
  const widget = event.target.closest('.widget'); if (!widget || event.button !== 0 || event.target.closest('.resize-handle') || event.target.closest('button,input,select,textarea,a,.person')) return;
  event.preventDefault(); const rect = widget.getBoundingClientRect(); const marker = document.createElement('div'); marker.className = 'drop-marker'; marker.style.gridColumn = widget.style.gridColumn; marker.style.gridRow = widget.style.gridRow; widget.after(marker);
  const preview = widget.cloneNode(true); preview.className = `${widget.className} drag-preview`; Object.assign(preview.style, { left: `${rect.left}px`, top: `${rect.top}px`, width: `${rect.width}px`, height: `${rect.height}px`, gridColumn: '', gridRow: '' }); document.body.append(preview); widget.classList.add('dragging');
  const offsetX = event.clientX - rect.left; const offsetY = event.clientY - rect.top;
  const move = (e) => { preview.style.left = `${e.clientX - offsetX}px`; preview.style.top = `${e.clientY - offsetY}px`; const gridRect = grid.getBoundingClientRect(); const gap = 20; const cellWidth = (gridRect.width - gap * 3) / 4; const col = Math.max(1, Math.min(5 - Number(widget.dataset.cols || 2), Math.floor((e.clientX - gridRect.left) / (cellWidth + gap)) + 1)); const row = Math.max(1, Math.floor((e.clientY - gridRect.top) / 165) + 1); marker.dataset.col = col; marker.dataset.row = row; marker.style.gridColumn = `${col} / span ${widget.dataset.cols || 2}`; marker.style.gridRow = `${row} / span ${widget.dataset.rows || 2}`; };
  const up = () => { widget.dataset.col = marker.dataset.col || widget.dataset.col || 1; widget.dataset.row = marker.dataset.row || widget.dataset.row || 1; widget.style.gridColumn = `${widget.dataset.col} / span ${widget.dataset.cols || 2}`; widget.style.gridRow = `${widget.dataset.row} / span ${widget.dataset.rows || 2}`; marker.remove(); widget.classList.remove('dragging'); preview.remove(); document.removeEventListener('pointermove', move); document.removeEventListener('pointerup', up); saveWidgetLayout(); };
  document.addEventListener('pointermove', move); document.addEventListener('pointerup', up, { once: true });
});
grid.addEventListener('pointerdown', (event) => {
  const handle = event.target.closest('.resize-handle'); if (!handle) return; event.preventDefault(); event.stopPropagation(); const widget = handle.closest('.widget'); const startX = event.clientX; const startY = event.clientY; const startCols = Number(widget.dataset.cols || 2); const startRows = Number(widget.dataset.rows || (widget.dataset.widget === 'people' ? 4 : 2)); widget.classList.add('resizing');
  handle.setPointerCapture?.(event.pointerId); const move = (e) => { const cellWidth = Math.max(grid.clientWidth / 4, 100); const cols = Math.max(1, Math.min(5 - Number(widget.dataset.col || 1), startCols + Math.round((e.clientX - startX) / cellWidth))); const rows = Math.max(1, Math.min(6, startRows + Math.round((e.clientY - startY) / 165))); widget.dataset.cols = cols; widget.dataset.rows = rows; widget.style.gridColumn = widget.dataset.col ? `${widget.dataset.col} / span ${cols}` : `span ${cols}`; widget.style.gridRow = widget.dataset.row ? `${widget.dataset.row} / span ${rows}` : `span ${rows}`; };
  const up = () => { widget.classList.remove('resizing'); document.removeEventListener('pointermove', move); saveWidgetLayout(); }; document.addEventListener('pointermove', move); document.addEventListener('pointerup', up, { once: true });
});
restoreWidgets();
function tick() { const now = new Date(); $$('.clock').forEach((widget) => { const timezone = widget.dataset.timezone; widget.querySelector('.clock-label').textContent = (widget.dataset.label || 'Local time').toUpperCase(); widget.querySelector('.clock-time').textContent = now.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit', timeZone: timezone }); widget.querySelector('.clock-date').textContent = now.toLocaleDateString([], { weekday: 'long', day: 'numeric', month: 'long', timeZone: timezone }); }); const h = now.getHours(); $('#daypart').textContent = h < 12 ? 'morning' : h < 18 ? 'afternoon' : 'evening'; } tick(); setInterval(tick, 1000); boot();
